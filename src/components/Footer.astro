---
import { site } from '../content/site';
import Icon from './Icon.astro';

const socials = [
  { name: 'LinkedIn', href: site.social.linkedin, icon: 'linkedin' as const },
  { name: 'Instagram', href: site.social.instagram, icon: 'instagram' as const },
  { name: 'YouTube', href: site.social.youtube, icon: 'youtube' as const },
];
---
<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <a href="#top" class="footer__name signature">{site.name}</a>
      <p class="footer__tag">{site.hero.kicker}</p>
    </div>

    <div class="footer__right">
      <a class="footer__contact" href={`mailto:${site.contactEmail}`}>
        <Icon name="mail" size={18} />
        <span>{site.contactEmail}</span>
      </a>
      <ul class="footer__social">
        {socials.map((s) => (
          <li>
            <a href={s.href} aria-label={s.name} target="_blank" rel="noopener noreferrer">
              <Icon name={s.icon} size={20} />
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>

  <div class="container footer__old">
    <p class="footer__old-text">{site.oldSite.label}</p>
    <a class="footer__old-link" href={site.oldSite.url} target="_blank" rel="noopener noreferrer">
      Visit the old site <Icon name="arrow-up-right" size={15} />
    </a>
  </div>

  <div class="container footer__base">
    <p>{site.copyright}</p>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--line);
    background: var(--paper-raised);
    padding-block: clamp(2.75rem, 6vw, 3.75rem) 1.75rem;
  }
  .footer__inner {
    display: flex;
    flex-wrap: wrap;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1.75rem 1.5rem;
  }
  .footer__name {
    font-family: var(--font-script);
    font-size: 2.3rem;
    line-height: 1;
    color: var(--ink);
    transition: color 0.18s ease;
  }
  .footer__name:hover { color: var(--accent-text); }
  .footer__tag {
    margin-top: 0.6rem;
    font-size: 0.82rem;
    letter-spacing: 0.05em;
    color: var(--ink-faint);
  }

  .footer__right {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.1rem;
  }
  .footer__contact {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
    color: var(--ink-soft);
    transition: color 0.18s ease;
  }
  .footer__contact:hover { color: var(--accent-text); }

  .footer__social { display: flex; gap: 0.6rem; }
  .footer__social a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.6rem;
    height: 2.6rem;
    border-radius: 50%;
    color: var(--ink-soft);
    border: 1px solid var(--line);
    background: var(--card);
    transition: color 0.18s ease, border-color 0.18s ease, transform 0.18s ease;
  }
  .footer__social a:hover {
    color: var(--ink);
    border-color: var(--line-strong);
    transform: translateY(-2px);
  }

  .footer__base {
    margin-top: clamp(2rem, 5vw, 3rem);
    padding-top: 1.5rem;
    border-top: 1px solid var(--line);
  }
  .footer__base p { font-size: 0.85rem; color: var(--ink-faint); }

  .footer__old {
    margin-top: clamp(2rem, 5vw, 3rem);
    padding: 1.15rem 1.4rem;
    background: var(--card);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 0.6rem 1.5rem;
  }
  .footer__old-text {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1rem;
    line-height: 1.4;
    color: var(--ink-soft);
    max-width: 52ch;
  }
  .footer__old-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-weight: 600;
    font-size: 0.9rem;
    color: var(--teal-text);
    white-space: nowrap;
  }
  .footer__old-link:hover { color: var(--teal-strong); }

  @media (min-width: 620px) {
    .footer__right { align-items: flex-end; }
  }
</style>
